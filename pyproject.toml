[build-system]
requires=["setuptools>=69"]
build-backend="setuptools.build_meta"
[project]
name="envcheck"
version="0.1.0"
description="Environment configuration hygiene checker"
requires-python=">=3.10"
[project.scripts]
envcheck="envcheck.cli:main"
[tool.setuptools.packages.find]
where=["src"]
