# EnvCheck

Configuration hygiene checker for `.env` files and environment-driven application settings. It flags missing values, duplicates, weak placeholders, and accidental formatting errors without printing secret values.

Credits: https://guns.lol/meduu
